# Held-out semantic evaluation

This directory is for merchant-permitted examples used to measure live Jev, not the deterministic fake.

Do not check in personalization text. The live runner must be opt-in and must not convert expected labels into fabricated provider responses.

Not run in this repository without TypeSafe credentials and authorized data.
